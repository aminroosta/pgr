use crate::pg_func::{PgArg, PgFunc};
use warp::Filter;

// pub fn to_route(
//     func: &PgFunc,
// ) -> impl Filter<Extract = (impl warp::Reply,), Error = warp::Rejection> + Clone {
//     // let (method, path, query) = PgFunc::parse_name(&func.name).unwrap();

//     // warp::path(func.)

// }
